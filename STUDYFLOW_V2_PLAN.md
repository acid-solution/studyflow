# StudyFlow V2 已确认设计

版本日期：2026-09-23

## 定位

StudyFlow 将长期学习目标拆成可执行计划，并用真实执行数据支持每日推进与周度复盘。它既可以独立使用，也可以在后续接收 JobPilot 经用户确认的学习计划。

## 已确认的核心模型

```text
Goal
├─ Goal（不限层数的分目标）
└─ Plan（可同时存在多个）
   └─ PlanVersion（V1、V2……，同一时间一个生效版）
      ├─ Milestone
      └─ Task
         └─ StudySession
```

- 计划可以挂在目标树的任意一级。
- 父目标不会自动完成；系统只返回完成提示，最终由用户确认。
- 目标使用 `active / achieved / abandoned` 保存历史，不物理删除。
- 计划创建后固定为按日期或按课时两种模式之一。
- 日期任务只安排到天；课时计划按顺序推进下一项。
- 调整整体路线时复制新版本，旧版本和执行记录保留。
- 激活新版本前检查基础结构版本和运行中计时，冲突返回 409。
- 任务使用整数版本做乐观锁；改期另存事件，保留原日期和新日期。
- 每个用户同时只能运行一个计时，开始和结束时间均由服务器生成。

## 系统边界

- StudyFlow 业务数据使用 MySQL。
- shared-auth 独立使用 PostgreSQL，负责邮箱账号、密码、刷新会话和访问令牌。
- StudyFlow 通过 JWKS 本地验证 `aud=studyflow` 的 RS256 JWT，并从 `sub` 获取用户 UUID。
- 首轮不使用 Redis、消息队列和业务微服务拆分。
- JobPilot 导入与 MCP 只保留产品入口，本轮不实现跨系统写入。

## 当前完成范围

- 递归目标、目标移动和循环引用阻止。
- 并行计划、日期/课时模式、版本草稿、复制和激活事务。
- 阶段、任务、首页四类任务、完成、重新打开、取消和改期。
- 单用户唯一运行计时、结束、丢弃和完成任务自动停止。
- 周度复盘和用户时区/周起始日偏好。
- Goose Migration、用户隔离、统一错误、健康检查和结构化日志。
- shared-auth JWKS 验证与受众隔离。
- 登录、目标、计划、任务、今日、复盘和设置的真实前端。
- Go、数据库集成、前端组件、类型检查和生产构建测试。

## 后续范围

1. JobPilot 通过 MCP 提交计划草稿，用户确认后事务导入。
2. 为跨系统导入加入幂等键、失败回滚和操作审计。
3. 根据真实性能数据决定是否缓存工作台或周报。
4. 增加更完整的端到端浏览器测试与部署配置。

## 验收原则

- 能用两个用户验证资源隔离。
- 能创建任意深度目标树和多个并行计划。
- 能激活计划、执行日期和课时任务、改期、计时并看到周报变化。
- 服务重启后业务数据仍在。
- `go test ./...`、前端测试、类型检查与生产构建全部通过。
